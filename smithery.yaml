startCommand:
  type: http
  configSchema:
    type: object
    required:
      - MCP_KEY
    properties:
      MCP_KEY:
        type: string
        title: "MCP Key"
        description: "API key for MCP / AXYS authentication"
